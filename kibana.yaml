kibana:
  image: docker.elastic.co/kibana/kibana:8.15.0
  ports:
    - "5601:5601"
  environment:
    - SERVER_HOST=0.0.0.0
    - SERVER_PORT=5601
    - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
  depends_on:
    - elasticsearch